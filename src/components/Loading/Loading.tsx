import * as Style from './Loading.styled';

const Loading = () => {
    return (
        <Style.Loading>
            <Style.ProgressSpin />
        </Style.Loading>
    );
};

export default Loading;
